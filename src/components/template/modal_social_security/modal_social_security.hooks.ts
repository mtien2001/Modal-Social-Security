import { dataMock } from "./modal_social_security.contants";
import { IModalSocialSecurity } from "./modal_social_security.types";

export const useModalSocialSecurity = (props: IModalSocialSecurity) => {
  return dataMock;
};
